services:
  postgres:
    image: postgres:17
    env_file:
      - .env.local
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: password
    ports:
      - "${DB_PORT}:5432"
