# Database
DB_HOST=(optional)
DB_PORT=5432
DB_NAME=Replace it with the Database name
DB_USER=Replace it with the Username
DB_PASSWORD=Replace it with the Password

# HikariCP
HIKARI_MAX_POOL_SIZE=
HIKARI_MIN_IDLE=
HIKARI_CONNECTION_TIMEOUT=